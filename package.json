{
  "name": "pi-agent-bun",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/run-workflow.ts",
    "compile": "bun build --compile src/run-workflow.ts --outfile dist/pi-agent-bun",
    "postinstall": "node scripts/patch-pi-config.ts"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.51.6",
    "@sinclair/typebox": "^0.34.4",
    "dotenv": "^16.4.5",
    "env-paths": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8"
  }
}
